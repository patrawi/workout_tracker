GEMINI_API_KEY=your_gemini_api_key_here
DATABASE_URL=postgresql://user:password@localhost:5432/workout_tracker
MASTER_PASSWORD=your_secret_password_here
JWT_SECRET=some-random-secret-string
# Comma-separated origins allowed to make credentialed cross-origin requests.
# Defaults to the deployed domain + localhost if unset.
ALLOWED_ORIGINS=https://workout.patrawi.com,http://localhost:3000
