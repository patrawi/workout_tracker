# Treat the weekly menu as a soft prior

Use the announced weekly canteen menu to rank likely matches, never to reject food outside that list. The user may skip a meal or consume an Off-Menu Meal, so a hard closed set would misrepresent real behavior; weekly-menu, off-menu, and no-log paths remain distinct.
