# Report plausible ranges before calibration

V1 reports low, central, and high values as a Plausible Nutrition Range rather than naming them P10, P50, and P90. The system lacks enough nutrient ground truth to demonstrate percentile coverage, so explicit heuristic uncertainty is more honest; calibrated probability intervals may replace these labels only after validation data supports that claim.
