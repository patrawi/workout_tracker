# Use constrained interval calculation in V1

V1 computes each Plausible Nutrition Range by minimizing, centralizing, and maximizing nutrient formulas under mass-balance and evidence constraints. Monte Carlo propagation is deferred until input distributions are supported by calibration data; repeated sampling from unvalidated priors would add complexity without adding evidence.
