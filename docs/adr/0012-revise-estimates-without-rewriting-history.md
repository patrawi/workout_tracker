# Revise estimates without rewriting history

Resolving a Reference-Pending Meal or adopting a newly available reference creates a proposed Nutrition Estimate Revision that requires explicit user confirmation. Confirmed nutrition history is never silently recomputed when the catalog changes; each revision retains its provider and reference version so historical results remain reproducible and auditable.
