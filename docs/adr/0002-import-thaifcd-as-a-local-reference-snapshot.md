# Import ThaiFCD as a local reference snapshot

The system imports the available ThaiFCD CSV into a versioned, queryable local snapshot and selects only relevant records for each weekly menu at runtime. We chose this over live website access or sending the complete dataset to a model so nutrition estimation remains reproducible, source-attributed, independent of website availability, and economical in model context.
